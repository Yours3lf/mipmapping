#version 430 core

layout(binding=0) uniform sampler2D result;

layout(location=0) uniform float lod;

in vec2 texcoord;

layout(location=0) out vec4 color;

void main()
{
  vec2 coord = vec2( texcoord.x, 1-texcoord.y );
  color = textureLod( result, coord, lod );
}
